# Metrial realtime service

This TypeScript Socket.IO service delivers Laravel-authorized, tenant-scoped
events. Run `npm ci && npm run typecheck && npm test && npm run build` from this
directory. It has no database access and does not accept business commands.
